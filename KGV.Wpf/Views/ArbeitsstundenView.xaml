<UserControl x:Class="KGV.Views.ArbeitsstundenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource KgvBackgroundBrush}">

    <Grid Margin="30">
        <StackPanel>
            <DockPanel LastChildFill="False">
                <TextBlock Text="Arbeitsstunden" FontSize="24" FontWeight="Bold" DockPanel.Dock="Left" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="Neu" Width="120" Height="30" Margin="0,0,10,0"
                            Command="{Binding NeueArbeitsstundeCommand}" />
                    <Button Content="Bearbeiten" Width="120" Height="30"
                            Command="{Binding BearbeitenCommand}" />
                </StackPanel>
            </DockPanel>

            <TextBlock Margin="0,10,0,0">
                <Run Text="Mitglied: " />
                <Run Text="{Binding Hauptmitglied.DisplayName}" FontWeight="SemiBold" />
            </TextBlock>

            <GroupBox Header="Pflichtstunden (Saison)" Margin="0,12,0,0">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Soll" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Pflichtstunden.Sollstunden}" FontWeight="SemiBold" />

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Geleistet" />
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding Pflichtstunden.Geleistet}" FontWeight="SemiBold" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Offen" Margin="0,6,0,0" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Pflichtstunden.Offen}" Margin="0,6,0,0" FontWeight="SemiBold" />

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Fehlbetrag" Margin="0,6,0,0" />
                    <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding FehlbetragText}" Margin="0,6,0,0" FontWeight="SemiBold" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Grund" Margin="0,6,0,0" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding Pflichtstunden.Befreiungsgrund}" Margin="0,6,0,0" TextWrapping="Wrap" />
                </Grid>
            </GroupBox>

            <DataGrid ItemsSource="{Binding Arbeitsstunden}"
                      SelectedItem="{Binding SelectedArbeitsstunde}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      Margin="0,15,0,0"
                      Height="520">
                <DataGrid.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding BearbeitenCommand}" />
                </DataGrid.InputBindings>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Nachname}" Width="140" />
                    <DataGridTextColumn Header="Vorname" Binding="{Binding Vorname}" Width="140" />
                    <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=d}" Width="110" />
                    <DataGridTextColumn Header="Saison" Binding="{Binding SaisonJahr}" Width="80" />
                    <DataGridTextColumn Header="Stunden" Binding="{Binding Stunden}" Width="80" />
                    <DataGridTextColumn Header="Beschreibung" Binding="{Binding Beschreibung}" Width="*" />
                    <DataGridCheckBoxColumn Header="Freigabe" Binding="{Binding Freigegeben}" Width="80" />
                    <DataGridTextColumn Header="Freigegeben am" Binding="{Binding FreigegebenAm, StringFormat=g}" Width="140" />
                    <DataGridTextColumn Header="Freigegeben von" Binding="{Binding FreigegebenVonName}" Width="160" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
