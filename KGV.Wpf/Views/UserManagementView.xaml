<UserControl x:Class="KGV.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource KgvBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False">
            <TextBlock Text="Nutzerverwaltung" FontSize="24" FontWeight="Bold" DockPanel.Dock="Left" />
            <Button DockPanel.Dock="Right" Content="Aktualisieren" Width="160" Height="35" Command="{Binding RefreshCommand}" />
        </DockPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <TextBlock Text="Suche:" VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBox Width="350" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text=" " Width="20" />
            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" />
        </StackPanel>

        <Border Grid.Row="2" Margin="0,0,0,10" Padding="10" Background="{StaticResource KgvSurfaceBrush}">
            <StackPanel>
                <TextBlock Text="{Binding ContextMemberTitle}" FontWeight="SemiBold" />
                <TextBlock>
                    <Run Text="E-Mail (Mitglied): " />
                    <Run Text="{Binding ContextMemberEmail}" />
                </TextBlock>
                <TextBlock>
                    <Run Text="Nutzerkonto: " />
                    <Run Text="{Binding ContextMemberAccountStatus}" />
                </TextBlock>

                <Separator Margin="0,8,0,8" />

                <TextBlock Text="{Binding SelectedUserTitle}" FontWeight="SemiBold" />
                <TextBlock>
                    <Run Text="User-ID: " />
                    <Run Text="{Binding SelectedUserId}" />
                </TextBlock>
                <TextBlock>
                    <Run Text="Rolle: " />
                    <Run Text="{Binding SelectedUserRole}" />
                </TextBlock>
                <TextBlock>
                    <Run Text="E-Mail (Mitglied): " />
                    <Run Text="{Binding SelectedUserEmail}" />
                </TextBlock>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Results}"
                  SelectedItem="{Binding Selected}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="User-ID" Binding="{Binding UserId}" Width="260" />
                <DataGridTextColumn Header="Rolle" Binding="{Binding Role}" Width="120" />
                <DataGridTextColumn Header="Mitglied" Binding="{Binding MitgliedDisplay}" Width="2*" />
                <DataGridTextColumn Header="E-Mail (Mitglied)" Binding="{Binding Email}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
