<UserControl x:Class="KGV.Views.SaisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource KgvBackgroundBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />

        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                    <Setter Property="IsReadOnly" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="Height" Value="35" />
            <Setter Property="Width" Value="140" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,16">
            <TextBlock Text="Saison" FontSize="24" FontWeight="Bold" DockPanel.Dock="Left" />

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Neu" Command="{Binding NewSaisonCommand}" />
                <Button Content="Bearbeiten" Command="{Binding ToggleEditCommand}" />
            </StackPanel>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="900">

                <TextBlock Text="Saison auswählen" FontWeight="SemiBold" />
                <ComboBox ItemsSource="{Binding Saisons}"
                          SelectedItem="{Binding SelectedSaison}"
                          DisplayMemberPath="Jahr"
                          Margin="0,4,0,0">
                    <ComboBox.Style>
                        <Style TargetType="{x:Type ComboBox}">
                            <Setter Property="IsEnabled" Value="True" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                    <Setter Property="IsEnabled" Value="False" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>

                <TextBlock Text="{Binding StatusMessage}" Foreground="DarkRed" Margin="0,10,0,0" TextWrapping="Wrap" />

                <GroupBox Header="Details" Margin="0,14,0,0">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Jahr" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding JahrText, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Pflichtstunden Soll" Margin="0,6,0,0" />
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PflichtstundenSollText, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,0" />

                        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="€ pro Fehlstunde" Margin="0,6,0,0" />
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding EuroProFehlstundeText, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,0" />

                        <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" Text="Bemerkung" Margin="0,6,0,0" />
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding BemerkungText, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,0" AcceptsReturn="True" Height="90" TextWrapping="Wrap" />
                    </Grid>
                </GroupBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="Speichern" Command="{Binding SaveCommand}" />
                    <Button Content="Abbrechen" Command="{Binding CancelCommand}" />
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
