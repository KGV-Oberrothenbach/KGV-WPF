<Window x:Class="KGV.Views.ResetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:KGV.Wpf.Helpers"
        Title="Passwort setzen" Height="260" Width="420"
        KeyDown="Window_KeyDown">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Neues Passwort" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="1" x:Name="NewPasswordBox" Height="30"
                     PasswordChanged="NewPasswordBox_PasswordChanged" />

        <TextBlock Grid.Row="2" Text="Passwort bestätigen" Margin="0,10,0,5"/>
        <PasswordBox Grid.Row="3" x:Name="ConfirmPasswordBox" Height="30"
                     PasswordChanged="ConfirmPasswordBox_PasswordChanged" />

        <StackPanel Grid.Row="4" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Abbrechen" Command="{Binding CancelCommand}" Margin="0,0,10,0" />
                <Button Content="Speichern" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" />
            </StackPanel>

            <TextBlock Margin="0,10,0,0" Text="{Binding StatusMessage}" Foreground="Red"/>
        </StackPanel>
    </Grid>
</Window>
