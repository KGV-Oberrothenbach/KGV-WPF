<Window x:Class="KGV.Views.ChangeEmailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mailadresse ändern" Height="300" Width="460"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Aktuelle E-Mail" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding CurrentEmail}" IsReadOnly="True" Height="30" />

        <TextBlock Grid.Row="2" Text="Neue E-Mail" Margin="0,10,0,5"/>
        <TextBox Grid.Row="3" Text="{Binding NewEmail, UpdateSourceTrigger=PropertyChanged}" Height="30" />

        <Button Grid.Row="4" Content="Code senden" Command="{Binding SendCodeCommand}" Margin="0,10,0,0" Height="32" />

        <TextBlock Grid.Row="5" Text="OTP-Code" Margin="0,10,0,5"/>
        <TextBox Grid.Row="6" Text="{Binding OtpCode, UpdateSourceTrigger=PropertyChanged}" Height="30"
                 IsEnabled="{Binding IsCodeSent}" />

        <StackPanel Grid.Row="7" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Abbrechen" Command="{Binding CancelCommand}" Margin="0,0,10,0" />
                <Button Content="Bestätigen" Style="{StaticResource PrimaryButton}" Command="{Binding VerifyCommand}" />
            </StackPanel>

            <TextBlock Margin="0,10,0,0" Text="{Binding StatusMessage}" Foreground="Red" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
