<Window x:Class="KGV.Views.UpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update verfügbar"
        SizeToContent="Height"
        Width="520"
        MinHeight="220"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Es ist eine neue Version verfügbar." FontSize="16" FontWeight="SemiBold" />

        <TextBlock Grid.Row="1" Margin="0,10,0,0" TextWrapping="Wrap">
            <Run Text="Aktuell: " />
            <Run x:Name="CurrentVersionRun" FontWeight="SemiBold" />
            <Run Text="  |  Online: " />
            <Run x:Name="OnlineVersionRun" FontWeight="SemiBold" />
        </TextBlock>

        <Border Grid.Row="2" Margin="0,12,0,0" Padding="10" Background="#0A000000" BorderBrush="#33000000" BorderThickness="1" CornerRadius="6">
            <TextBlock x:Name="NotesText" TextWrapping="Wrap" />
        </Border>

        <StackPanel Grid.Row="3" Margin="0,16,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Später" MinWidth="110" Margin="0,0,10,0" Click="Later_Click" />
            <Button Content="Herunterladen" MinWidth="140" Click="Download_Click" />
        </StackPanel>
    </Grid>
</Window>
